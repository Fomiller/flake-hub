{% if argocd.enabled and "dev" in argocd.environments %}# Written once, then it is yours. `nix run .#generate` never touches it again.
#
# dev overrides, layered on ../values.app.base.yaml. Only keys that differ from
# the base belong here.

image:
  tag: latest
{% endif %}
