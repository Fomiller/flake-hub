{% if argocd.enabled and argocd.environment == "staging" %}# Written once, then it is yours. `nix run .#generate` never touches it again.
#
# staging overrides, layered on ../values.app.base.yaml. Only keys that differ
# from the base belong here. The deployed image tag is the usual one:
#
#   image:
#     tag: v1.2.3
{% endif %}
